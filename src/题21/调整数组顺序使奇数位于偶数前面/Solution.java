package 题21.调整数组顺序使奇数位于偶数前面;/** * 思路：1.遍历数组得到奇数个数 *      2.copy一个数组 *      3.遍历copy的数组， *          如果为奇数，放在array数组的前列 *          如果为偶数，放在下标为oddCnt的后面 */public class Solution {    public void reOrderArray(int [] array) {        if ( array==null || array.length==0 ){            return;        }        //奇数个数        int oddCnt = 0;        for (int x : array){            if (!isEven(x)){                oddCnt++;            }        }        int i = 0;        int j = oddCnt;        int[] copy = array.clone();            for (int num : copy){            if (num % 2 == 1) {                array[i++] = num;            }            else {                array[j++] = num;            }        }    }    private boolean isEven(int x){        return x % 2 == 0;    }}